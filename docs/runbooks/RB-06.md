# RB-06 — Local-RPC breaker trips (OD-9)

Source: HLD v0.5 §9.1. Exercised by the degradation suite (§10.2) by following these steps.

**Two rules hold in every runbook.** Never restore service by weakening a control (disabling the
sandbox, relaxing a budget, raising a rate cap, skipping a check, hand-editing a bundle). A needed
change goes through change control (plan §17.1). `handed_off` episodes are never reopened.

| | |
|---|---|
| Trigger | Local-RPC breaker trips (OD-9) |
| Automatic behaviour | RPC-backed polling suspended; telemetry-health signal raised. |
| Operator actions | Check validator RPC load and whether observation is the cause (Q-7). Do not raise P-14 ad hoc. |
| Exit condition | Latency below P-16 for W samples; polling resumes automatically. |
| Where to look | daemon `/health` breaker state |
